---
# ansible/roles/ojp_proxy/handlers/main.yml

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: restart ojp-server
  ansible.builtin.systemd:
    name: ojp-server
    state: restarted
  become: true

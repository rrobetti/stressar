# ansible/vars/prod-pgbouncer-thin-client.yml
#
# Secondary control profile (not the headline production comparison).

bench_replica_count:     16
bench_target_rps:        500
bench_warmup_seconds:    300
bench_duration_seconds:  1800
bench_cooldown_seconds:  120
bench_repetitions:       5
bench_slo_p95_ms:        50

pgbouncer_pool_size:      16
pgbouncer_min_pool_size:  16
pgbouncer_reserve_pool_size: 0
pgbouncer_local_pool_size: 2
benchmark_profile_name: "pgbouncer-thin-client"

# ansible/vars/prod-hikari-equal-budget-48.yml
#
# Secondary control profile (not the headline production comparison).

bench_replica_count:     16
bench_target_rps:        500
bench_warmup_seconds:    300
bench_duration_seconds:  1800
bench_cooldown_seconds:  120
bench_repetitions:       5
bench_slo_p95_ms:        50

bench_db_connection_budget:             48
bench_hikari_max_pool_size_per_replica: 3
benchmark_profile_name: "hikari-equal-budget-48"
